function [x] = steepest_descent(a, f, gradf, TOL)

    % a = tebakan awal
    % f = fungsi yang ingin dioptimisasi
    % gradf = gradien dari f
    % TOL = toleransi dari iterasi

    p = 

    g = ...

    fd_g = diff(g)

    while a < TOL
        % sekip anjeng

endfunction
